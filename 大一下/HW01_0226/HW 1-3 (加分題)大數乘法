# 🧮 加分題：大數乘法 (Big Number Multiplication)

在 C++ 中，`int` 的上限約為 $2 \times 10^9$，即便使用 `long long` 也無法處理超過 20 位的運算。本實作的核心在於將數字拆解為單個數位，利用**陣列**作為存儲媒介，手動模擬人類直式乘法的計算過程。

---

## 🔍 核心觀念解密

### 1. 為什麼要「反向放入」陣列？
這是本程式最關鍵的設計。當我們輸入一個數字（例如 `123`），在字面上 `1` 是最高位，`3` 是個位數。

* **問題**：如果正向存放（`arr[0]=1, arr[1]=2, arr[2]=3`），當運算發生進位時，我們必須把整條陣列往後挪才能騰出空間給最高位，效率極低。
* **解法**：將個位數放在 `arr[0]`（即 `arr[0]=3, arr[1]=2, arr[2]=1`）。
* **優點**：
    * **對齊簡單**：不論數字多長，`arr[0]` 永遠是對齊的個位數。
    * **進位自然**：進位是往更高位（索引增加的方向）傳遞，符合陣列增長的邏輯。

### 2. 交叉相乘的數學原理
在直式乘法中，被乘數的第 $i$ 位與乘數的第 $j$ 位相乘，其結果貢獻在積的第 $i+j$ 位。

* **公式推導**：
    若 $A = \sum a_i \cdot 10^i$ 且 $B = \sum b_j \cdot 10^j$
    則 $A \times B = \sum \sum (a_i \cdot b_j) \cdot 10^{i+j}$
* **程式實作**：
    ```cpp
    sum[i + j] = sum[i + j] + arr1[i] * arr2[j];
    ```
    這行代碼在雙重迴圈中不斷累加相同位數的乘積，暫時不處理進位，等全部乘完後再統一處理。

---

## 💡 註解牆版程式碼 (加強觀念版)

```cpp
#include<iostream>
using namespace std;

int main(void) {
    // 【階段一：被乘數讀取】
    int n;
    cin >> n;
    int* arr1 = (int*)malloc(n * sizeof(int));
    // 技巧：i 從 n-1 跑到 0，讓最後輸入的個位數存入 arr1[0]
    for (int i = n - 1; i >= 0; i--) cin >> arr1[i]; 

    // 【階段二：乘數讀取】
    int m;
    cin >> m;
    int* arr2 = (int*)malloc(m * sizeof(int));
    for (int i = m - 1; i >= 0; i--) cin >> arr2[i];

    // 【階段三：交叉相乘】
    // 兩數相乘之位數最多為兩者位數相加 (n + m)
    int* sum = (int*)malloc((m + n) * sizeof(int));
    for (int k = 0; k < (m + n); k++) sum[k] = 0;

    for (int i = 0; i < n; i++) {       // 跑遍被乘數每一位
        for (int j = 0; j < m; j++) {   // 跑遍乘數每一位
            // 將乘積累加至對應的位數 i+j
            sum[i + j] += arr1[i] * arr2[j]; 
        }
    }

    // 【階段四：統一進位處理】
    // 這一步是將「不合法」的數位(例如 45)轉換為「合法」的 5，並進位 4
    for (int i = 0; i < (m + n - 1); i++) {
        sum[i + 1] += (sum[i] / 10);    // 進位
        sum[i] %= 10;                   // 本位保留餘數
    }

    // 【階段五：反向輸出】
    // 因為 arr[0] 是個位，輸出時必須從最高索引倒著印回來
    int check = 0;
    for (int i = (m + n - 1); i >= 0; i--) {
        if (check == 0 && sum[i] == 0) continue; // 略過最高位多出的 0
        check = 1;
        cout << sum[i];
    }

    free(arr1); free(arr2); free(sum);
    return 0;
}
